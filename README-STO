Notes to me/anyone else:

You may need to run ./configure again (I ran it on OS X).
Then just run `make`

To run thttpd:
sudo ./thttpd
To run in the foreground:
sudo ./thttpd -D

You can test it by going to http://127.0.0.1